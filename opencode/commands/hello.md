---
description: Say hello and explain what this command demonstrates.
---

You are running the `/hello` command from `.opencode/commands/hello.md`.

Do three things:

1) Say hello.
2) Explain that commands are loaded from `.opencode/commands/` (project) or `~/.config/opencode/commands/` (global).
3) List the other extension examples included in this package: agent, skill, tool, theme, plugin.
